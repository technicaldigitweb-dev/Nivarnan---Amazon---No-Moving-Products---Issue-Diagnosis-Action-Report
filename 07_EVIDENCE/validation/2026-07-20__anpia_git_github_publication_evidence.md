# Evidence: ANPIA Git/GitHub Publication

**Date:** 2026-07-20
**Status:** PASS
**No credential value appears anywhere in this document.**

## 1. Repository

- **URL:** `https://github.com/technicaldigitweb-dev/Nivarnan---Amazon---No-Moving-Products---Issue-Diagnosis-Action-Report.git`
- **Remote name:** `origin`
- **Branch:** `main`
- **Repository state before this task:** No `.git` directory existed anywhere in the project
  (confirmed directly). The remote repository existed but was completely empty (`git ls-remote
  origin` returned no refs) -- there was no existing history to reconcile, diverge from, or
  force-push over.

## 2. Commit

- **Commit hash:** `41cf146b65243bc22dd60f7410b94ed33b7d38e0`
- **Message:** `feat(anpia): deliver production report and scheduler-ready pipeline`
- **Files committed:** 201
- **Insertions:** 20,842 / Deletions: 0 (root commit)
- **Timestamp:** 2026-07-20 09:54:28 +05:30 (author date, per project convention)

## 3. Push

- **Command:** `git push -u origin main`
- **Result:** `* [new branch]      main -> main`, exit code 0. No force flag used (none needed --
  remote had no prior history).
- **Local HEAD after push:** `41cf146b65243bc22dd60f7410b94ed33b7d38e0`
- **Remote HEAD after push (`origin/main`, via `git fetch` + `git rev-parse`):**
  `41cf146b65243bc22dd60f7410b94ed33b7d38e0`
- **Match:** YES -- identical.

## 4. Files committed (by top-level area)

All numbered AIOS folders (`00_PROJECT_CONTROL` through `12_ARCHIVE`) except the paths listed
in §5, plus `.gitignore`, `.env.example`, and `README.md`. Includes both required production
HTML assets (`09_OUTPUTS/html/production/2026-07-20_nivarnan_anpia_v001.html` and `.../v002.html`),
the manual update-to-table pipeline, the disabled automation deployment package, all daily
skill/summary files, all validation and evidence documents produced across the project, and the
final handover set.

## 5. Files excluded (disclosed staging decisions)

| Path / pattern | Reason |
|---|---|
| `.env` | Real credential file -- never committed, by design |
| `Sources/` (entire top-level folder) | Raw source intake, duplicated by curated copies already present inside the numbered folders (`01_REQUIREMENTS`, `02_SOURCE`, `08_SKILLS`); also the location of the two protected credential-template files (see `00_PROJECT_CONTROL/PROTECTED_SOURCE_INVENTORY.md`) |
| `*.xlsx`, `*.zip` (repo-wide) | Uploaded source workbooks/archives -- reference-only per this closure task's own file-type review guidance; kept local, evidence-only |
| `.claude/`, `.mcp.json` | Local Claude Code tooling configuration, not a project asset |
| `09_OUTPUTS/data/`, `09_OUTPUTS/logs/` | Raw per-run extraction data and logs (pre-existing ignore rule) |
| `09_OUTPUTS/html/*.html` (loose top-level files, `v001`-`v007` iteration builds) | Superseded intermediate builds; two (`v003`, `v004`) exceed 100 MB and would hard-fail a GitHub push; only the curated `production/` and `test_versions/` subfolders are approved assets |
| `__pycache__/`, `*.pyc` | Generated bytecode |

No file over 10 MB was staged. Largest staged files are the two approved production HTML
outputs at ~3.8 MB each -- well under GitHub's 100 MB hard limit and 50 MB soft-warning
threshold. Git LFS was not configured or needed.

## 6. Secret scan (staged content)

Performed via `git diff --cached` immediately before commit:

- **Exact-value scan** (known real host/password literals): 0 matches. CLEAN.
- **Pattern scan** (password assignment, DSN-with-credentials, private-key headers, GitHub/Slack
  token formats): 0 real matches (one incidental match was this evidence document's own text
  *describing* the pattern scan, not a credential).

Full detail in `07_EVIDENCE/validation/2026-07-20__anpia_pre_git_secret_remediation_evidence.md`.

## 7. Repository size

- `.git` object store after push: ~10 MB (246 objects).
- No large-file or LFS policy issue encountered.

## 8. Automation-inactive proof

- No `crontab`, no `systemctl`/systemd unit, and no Windows Scheduled Task named `anpia` exists
  on this machine (checked directly; this is a Windows development workstation, not the target
  VM). `05_IMPLEMENTATION/deployment/` files are committed as static, uninstalled artifacts only.
  `AUTOMATION_BUILT_NOT_ACTIVATED` confirmed unchanged by this task.

## 9. Git status after push

Working tree clean; zero untracked files remain outside the documented `.gitignore` exclusions.

## 10. Conclusion

Git initialization, staging, commit, and push all completed successfully on the first attempt,
with local and remote HEAD confirmed identical. No credential reached Git history at any point --
the pre-commit secret scan caught and remediated the exposure before the first commit was ever
created (see `07_EVIDENCE/validation/2026-07-20__anpia_pre_git_secret_remediation_evidence.md`).
